import React from 'react'
import "./ChannelsAndDMs.css"

export const ChannelsAndDMs = ( {id} ) => {
    const local_mook = JSON.parse(localStorage.getItem("MOOK"))
  return (
    <section className='accecibility'>
        <div className='top-btns'>
        </div>
    </section>

  )
}
